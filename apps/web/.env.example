# Server-only URL used by Next.js to reach Fastify.
API_BASE_URL=http://127.0.0.1:3001

# Optional public base URL for product storage keys returned by the catalog API.
# Leave empty to use /product-images/<storage-key> on the current host.
NEXT_PUBLIC_PRODUCT_IMAGE_BASE_URL=
